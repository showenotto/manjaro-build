---
- name: Backing up 'manjaro-build' to GitHub...
  git_config:
    name: user.email
    value: "showenotto@proton.me"
    scope: global
- name:
  shell: "git config --global user.name 'Showen' && git add . && git commit -m '1.0' && git push"
  args:
    chdir: "{{target_home_dir}}/manjaro-build/"
